# 組み込み向けソフトコアプロセッサ アーキテクチャ比較と `minc` の優位性

本ドキュメントは、FPGA上で実装される主要な組み込み向けプロセッサIPのアーキテクチャ系統（51系、RISC-V系、独自アーキテクチャ系）を比較し、独自開発の最小構成プロセッサ `minc` およびその専用Cコンパイラ `mincc` の学術的・技術的な立ち位置（新規性と優位性）を整理したものである。

## 1. 51系 (Intel 8051互換アーキテクチャ)
1980年代から存在する古典的で最も普及している8ビットMCUアーキテクチャの系統。

- **代表例**: CAST T8051XC3 など
- **規模感**: 約730 LUT （4.4k ASIC Gates換算）
- **メリット**:
  - **圧倒的な資産とエコシステム**: 過去数十年のノウハウ、既存のCコード資産、周辺IPが膨大に存在する。
  - **充実したツールチェーン**: Keilなどの強力な商用Cコンパイラやデバッガが揃っており、ソフトウェア開発が容易。
  - **制御用途への最適化**: ビット単位の操作やI/O制御に特化した命令を持っており、マイコン的な用途に強く適合する。
- **デメリット**:
  - **無駄の多いハードウェアコスト**: 基本設計が古く（CISC的）、FPGAのLUTやBRAM構造に最適化されていないため、デコーダや制御ロジックが複雑化し、サイズが膨らむ。

## 2. RISC-V系
オープンソース化された最新の命令セットアーキテクチャ (ISA)。学術界から産業界まで爆発的に普及している。

- **代表例**: 
  - **SERV**: 198 LUT / 164 FF （最小だがビットシリアル実行のため劇的に遅い）
  - **FemtoRV32 (quark)**: 約980 LUT （パラレル実行・最小構成）
  - **NEORV32**: 約2300 LUT / 1000 FF （RTOS対応の標準的SoC構成）
- **メリット**:
  - **世界標準のオープンISA**: ライセンスフリーであり、学術から商用まで幅広く利用可能。
  - **モダンで強力なコンパイラ**: GCCやLLVMが公式対応しており、C/C++やRustをフルサポート。高度な最適化が可能。
  - **命令のモジュール化**: `RV32I`（基本仕様）に、乗算や圧縮命令などを目的に応じて追加・削除できる。
- **デメリット**:
  - **最小状態でも「32ビット」の重さ**: 規約上、最低でも32ビットのデータ幅・レジスタ・命令長ベースとなるため、パラレル（並列）構成で作るとどうしても1000 LUT級まで膨らむ。
  - **超小型化における強烈なトレードオフ**: 極小リソースに押し込む場合（SERV等）、1命令を1ビットずつ処理する「ビットシリアル方式」を採用せざるを得ず、1命令に32クロック以上を要するなど実用的な速度が犠牲になる。

## 3. 独自アーキテクチャ系 (特化型・カスタムISA)
特定の用途やFPGA上の極小リソース活用などに合わせて、命令セットから独自に設計された系統。

- **代表例**: Xilinx PicoBlaze (KCPSM6)
- **規模感**: 約130 LUT / 76 FF
- **メリット**:
  - **究極のリソース最適化**: FPGA固有の構造にしっくりハマるように命令セットを設計できるため、汎用アーキテクチャでは不可能なレベルの小型化が可能。
  - **必要機能への特化**: 不必要な命令や複雑なアドレッシングモードを完全に排除できる。
- **デメリット**:
  - **ツールチェーンの欠如**: C言語の標準的なコンパイラが使えない。PicoBlazeが基本的にアセンブラ専用となっているのはこのためであり、ソフトウェアの生産性が著しく低い。

---

## 4. `minc` アーキテクチャの特異性と優位性
`minc` は、「汎用アーキテクチャのサイズ肥大化」と「独自アーキテクチャのC言語非対応」という両者の弱点を、**ハードウェア・ソフトウェアの協調設計（Co-design）** によって解決することを目的としたアーキテクチャである。

- **スペック**:
  - **アーキテクチャ**: 独自8-bitコア (18-bit命令幅, 16x8-bit汎用レジスタ)
  - **実行方式**: パラレル実行
  - **規模**: **371 LUT** / **77 FF**
  - **言語対応**: C言語ネイティブ対応 (専用コンパイラ `mincc`)

### `minc` の技術的ブレイクスルー
1. **「C言語が動く」限界のミニマリズム**
   専用コンパイラ `mincc` を同時に開発することで、局所変数（スタック）やポインタ操作などのC言語のセマンティクスを効率よく実行できる最小限の命令（ハードウェアスタックポインタ、`ldm`, `stm`, `call`等）だけを厳選。
2. **PicoBlaze級の面積と、実用的なパラレル速度の両立**
   RISC-V系で極小を目指すSERV（198 LUT）はビットシリアルで非常に遅いが、`minc` は398 LUTという小ささに収めつつ、**パラレル実行による良好な実行速度**を維持している。
3. **8051を凌ぐ軽量さと生産性**
   既存の8ビット汎用C対応コア（8051系）が700 LUT超えを要求するのに対し、約半分のリソースで同等以上のソフトウェア生産性（Cプログラミング）を実現した。

---

## 【付録】 FPGA向け主要CPUコア 回路規模・スペック比較表

| コア名称          | アーキテクチャ          | データ幅      | 実行方式      | 規模 (LUT / FF)※1         | C言語対応           | ライセンス等    | 特徴・出典                                            |
| ------------- | ---------------- | --------- | --------- | ----------------------- | --------------- | --------- | ------------------------------------------------ |
| **SERV**      | RISC-V (RV32I)   | 32-bit    | ビットシリアル   | **198 LUT / 164 FF**    | ◎ (GCC/LLVM)    | ISC (OSS) | 最小のRISC-Vだが、1命令に32サイクル以上かかるため劇的に遅い               |
| **PicoBlaze** | Xilinx独自         | 8-bit     | パラレル(2st) | **約130 LUT / 76 FF**    | × (アセンブラ前提)     | Xilinx専用  | 極小だがCコンパイラ対応構文を持たず、開発が困難                         |
| **minc**      | **独自**           | **8-bit** | **パラレル**  | **398 LUT / 42 FF**※2 | **◎ (専用mincc)** | オリジナル     | **PicoBlaze並みの軽量さを保ちながら、C言語で記述可能（スタック等ネイティブ対応）** |
| **CAST 8051** | 8051互換           | 8-bit     | パラレル      | **約730 LUT** （換算)       | ◯ (商用コンパイラ)     | IPベンダー    | 業界標準。C言語で書けるが、FPGA上ではややLUTを消費する                  |
| **FemtoRV32** | RISC-V (RV32I)   | 32-bit    | パラレル      | **約980 LUT**            | ◎ (GCC/LLVM)    | BSD (OSS) | パラレル構成にすると最小設定でも1000LUT弱を使用                      |
| **NEORV32**   | RISC-V (RV32IMC) | 32-bit    | パラレル      | **約2300 LUT / 1000 FF** | ◎ (GCC/LLVM)    | BSD (OSS) | RTOSなどが動く標準的な構成。高機能だが巨大                          |

> ※1 LUT数はFPGAのセル構造に依存するため代表値。
> ※2 `minc`におけるFF数は概算値。
